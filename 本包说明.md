# SOC SOH算法工具包 —— 交付说明

> 这份是「包裹层」说明：讲清这个文件夹是怎么攒出来的、三块内容怎么配起来跑。
> 产品本身的文档在根 `README.md`（总入口）、`算法库/README.md`（算法库）、
> `脚本/README.md`（工具链操作手册）。

---

## 一、这是什么

单节锂电池 **SOC（荷电状态）+ SOH（健康度）在线估计**的一整套闭环：

```
算法库/      MCU 端的算法库（纯 C99）          ← 跑在板子上
    ↕  UART：5 Hz 遥测行 + AA55 调参帧
脚本/        PC 端采集 / 控载 / 标定 / 验证     ← 命令行
上位机/       上面那套的图形界面（打包产物）      ← 双击就能用
```

参考硬件：单节 18650（NCR18650GA / 18650-3350D，3350 mAh）+ CH32X035 采集板
（INA226 + NTC + SSD1306）+ 一台程控电子负载**或可编程电源**（RS232 / SCPI）。
**算法库不绑定这套硬件**；控载脚本也只用通用 SCPI 根命令写法（充/放靠
`--charge` 切），换源/载品牌用 `--cmd` 覆盖几条命令即可。

## 二、三块内容的来源与规模

| 目录 | 是什么 | 规模 |
|---|---|---|
| `算法库/` | 算法库 14 个源文件 + `README` / `在线调参协议.md` / `移植与对齐指南.md` / `AI移植指令.md` + 8 个移植示例 | 26 文件 / 0.4 MB |
| `脚本/` | 采集 / 控载（充+放） / 拟合 / 标定 / SOH 验证 / EKF 调参 / 图形界面 / 常量对拍 + 真实测试数据 | 22 文件 / 3.9 MB |
| `上位机/` | `build_exe.py` 的 PyInstaller 产物（exe + `_internal` + 运行时脚本副本） | 317 文件 / 83.9 MB |

根目录的 `README.md` / `.gitignore` / `.gitattributes` 一并带上，可直接当仓库根用。

### 随包的实测数据（在 `脚本/` 里）

| 文件 | 大小 | 是什么 |
|---|---|---|
| `stair1.csv` | 2.9 MB | 十档「放电 → 静置」原始数据（MCU 5 Hz 遥测行），**不可再生，是这套工具链的验收基准** |
| `stair1_stair.csv` / `.json` / `_ocv.csv` | 1.9 KB / 12 KB / 396 B | `soc_bench stair` 的中间产物（拟合出的 R0 / R1 / τ / OCV 表） |
| `stair1_stair.png` / `_timeline.png` | 183 / 82 KB | 拟合结果图 |
| `stair1_soc_cmp.csv` | 100 KB | SOC 轨迹对比 |
| `pulse.csv` / `pulse_fit.png` | 176 KB / 50 KB | 单脉冲 RC 拟合的输入与结果 |

> `.png` 是正式结果图，不是临时产物；`stair1*.csv` 是**真实电芯实测数据**，删了就没了。

## 三、闭环怎么跑

**固件侧**只写一个函数就能跑，见根 `README.md`「三十秒上手」；
接到自己板子上的完整步骤见 `算法库/AI移植指令.md`（给 AI）或 `算法库/移植与对齐指南.md`（人工）。

**PC 侧**两种用法：

```bash
# 图形界面（推荐）
python 脚本/build_exe.py      # 打一次包，产物在仓库根 上位机/
上位机/锂电池上位机.exe         # 之后双击它

# 命令行
pip install -r tools/requirements.txt
python 脚本/soc_record.py --out stair1.csv      # 终端 B：采数据
python 脚本/soc_load.py --charge                # 终端 A：充电方向（要可编程电源）
python 脚本/soc_bench.py                        # 终端 A：向导 → 控载 → 十档充/放
python 脚本/soc_bench.py stair --csv stair1.csv # 容量/SOC/档数自动读档位计划
python 脚本/test/check_consts.py                # 固件 ↔ PC 副本常量对拍
```

完整测试流程（每一步该看什么、判据是什么）见 `tools/README.md` §4。

## 四、`上位机/` 特别说明

- 它是 **PyInstaller 打包产物**，不是源码。真正的源码是 `脚本/bms_gui.py` +
  `脚本/bms_tune_proto.py`。
- exe **优先加载同级的 `tools/` 里那份脚本**。所以只改了 `脚本/*.py`
  （除 `bms_gui.py` 以外）**不需要重新打包**，把新脚本覆盖过去就生效；
  改了 `bms_gui.py` 本身才要 `python 脚本/build_exe.py` 重打。
- `上位机/tools/` 是 exe 的运行时脚本副本；`上位机/_internal/` 是解释器与
  numpy / matplotlib / tcl-tk / Pillow。这两块都不能单独删。

## 五、拿它建 git 仓库

这个文件夹**本身不是** git 仓库，只是一份文件集合，直接 `git init` 即可
（`.gitignore` / `.gitattributes` 已就位）。

两点要注意：

1. `.gitignore` 里目前**排除了 `上位机/`**（约 83 MB 二进制，开源惯例不进版本库）。
   如果你就是要连 exe 一起提交，把 `.gitignore` 里
   `_build/` / `上位机/` / `*.spec` 那三行删掉。
2. `.gitattributes` 强制全仓库 LF。`算法库/` 下的 `.c/.h` 已统一为 **UTF-8**
   编码；工程若是 Keil / MounRiver 的 GBK 默认编码，接入前先
   `iconv -f UTF-8 -t GBK` 转码（改动只在注释里），别只转一部分 ——
   混着编码的工程在部分 IDE 里会显示乱码。

## 六、许可证

**MIT** —— 全文见根 `LICENSE`。

14 个核心源文件与 8 个移植示例的头部都带了许可声明；三份文档（根 `README.md`、
`算法库/README.md`、`算法库/移植与对齐指南.md`）里也都有对应章节。
可以自由用于商业闭源项目，只需保留版权声明与许可全文；软件按"现状"提供，不附带担保。

## 七、清点

| 项 | 值 |
|---|---|
| 总文件数 | 370 |
| 总体积 | 88.2 MB（Windows 口径，含 `上位机/` 的 83.9 MB） |
| 源码 | `算法库/` 14 个 `.c/.h` + 8 个示例 + `脚本/` 10 个 `.py` |
| 文档 | 根 README + 英文 README_EN + 算法库 4 份 + 工具链 1 份 |
| CI | `.github/workflows/ci.yml`（gcc 四组合编译 + UTF-8 校验 + 常量对拍） |
| 实测数据 | `脚本/` 内 2 个原始 + 4 个中间产物 + 3 张结果图 |
| 整理日期 | 2026-09-19（开源化整理） |
